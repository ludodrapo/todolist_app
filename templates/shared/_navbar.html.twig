<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary mb-5">
	<div class="container">
		<a class="navbar-brand" href="{{ path('homepage') }}">My lil' 2Do list</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>


		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('homepage') }}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{% if app.user %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Tâches</a>
						<div class="dropdown-menu">
							<a class="dropdown-item" href="{{ path('task_create') }}">
								Créer une tâche
							</a>
							<a class="dropdown-item" href="{{ path('task_list') }}">
								Liste des tâches
							</a>
						</div>
					</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Utilisateurs</a>
						<div class="dropdown-menu">
							<a class="dropdown-item" href="{{ path('user_create') }}">
								Créer un utilisateur
							</a>
							<a class="dropdown-item" href="{{ path('user_list') }}">
								Liste des utilisateurs
							</a>
						</div>
					</li>
				{% endif %}

			</ul>

			{% if app.user %}
				<div class="d-flex">
					<a href="{{ path('logout') }}" class="btn btn-light">
						<i class="bi bi-box-arrow-right"></i>
						Se déconnecter
					</a>
				</div>
			{% endif %}
		</div>
	</div>
</nav>
